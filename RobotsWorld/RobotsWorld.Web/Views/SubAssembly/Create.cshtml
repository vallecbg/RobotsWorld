@model SubAssemblyInputModel
@{
    ViewData["Title"] = "Create Sub Assembly";
    var error = ViewData[GlobalConstants.ModelError];
    var assemblyId = ViewData[GlobalConstants.AssemblyId].ToString();
}

<section class="banner-area" id="home">
    <div class="container">
        <div class="row fullscreen d-flex align-items-center justify-content-center">
            <div class="col-md-6">
                <h2 class="d-flex justify-content-center text-light">@ViewData["Title"]</h2>
                <hr class="h2" />
                <form enctype="multipart/form-data" class="mx-auto" asp-controller="SubAssembly" asp-action="Create" method="post">
                    @if (error != null)
                    {
                        <p class="text-danger">@error</p>
                    }
                    <div class="form-group text-center">
                        <label asp-for="Name" class="text-center text-light"></label>
                        <input asp-for="Name" class="form-control" placeholder="Name..." />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group text-center">
                        <label asp-for="Quantity" class="text-center text-light"></label>
                        <input asp-for="Quantity" class="form-control" placeholder="Quantity..." />
                        <span asp-validation-for="Quantity" class="text-danger"></span>
                    </div>
                    <div class="form-group text-center">
                        <label asp-for="Weight" class="text-center text-light"></label>
                        <input asp-for="Weight" class="form-control" placeholder="Weight..." />
                        <span asp-validation-for="Weight" class="text-danger"></span>
                    </div>
                    <div class="form-group text-center">
                        <label asp-for="Image" class="text-center text-light"></label>
                        <input type="file" asp-for="Image" class="form-control" name="Image" />
                        @if (ViewData.ContainsKey(GlobalConstants.Error))
                        {
                            <div class="row col-md-12">
                                <span class="row ml-1 text-danger ">@ViewData[GlobalConstants.Error]</span>
                            </div>
                        }
                    </div>
                    <input type="hidden" value="@assemblyId" asp-for="AssemblyId"/>

                    <hr class="h2" />

                    <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-info">Create</button>
                    </div>

                </form>
            </div>
        </div>
    </div>
</section>